# H003 neighborhood scale intervention

H002 completed all three seeds in run35271388310. All three archives and their 189 internal file digests were retrieved and checked. Standard beta rank smoothing produced stronger tree utility than the prior contextual interpolation, but a full table proximity check found roughly 78 percent of its records closer to training examples than the fifth percentile of real development distances. Wider beta smoothing reduced that fraction to roughly35 percent. These are development diagnostics, not probabilities of privacy violation or good data.

Test one training only intervention: choose a different beta kernel bandwidth in each context using the spacing of actual real training neighbors. Standard empirical beta smoothing, adaptive copula bandwidths and nearest neighbor interpolation have prior art. This particular local bandwidth rule is a hypothesis, not a priority claim.

For each of the232 wilderness, soil and cover contexts, compute all training leave one out nearest distances after global training standard deviation scaling. Produce one standard beta pilot using seed24017 plus the context index, assign the exact empirical marginal values, and measure all its nearest training distances. Set bandwidth to the squared ratio of median real leave one out distance to median pilot distance, clipped between1 and the context count. This uses an approximate square root relationship between variance and width; it does not guarantee a distance constraint. Singleton and constant contexts retain width1 and cannot be declared private.

Compare this adaptive beta candidate with exact five neighbor interpolation and with that same interpolation followed by contextual rank assignment. Use one anchor per training record, exclude the anchor from neighbor selection except singleton contexts, and keep original category frequencies. The raw interpolation uses weights uniform from0.1 to0.9. The other two variants preserve each context marginal exactly. No source records or generated records are dropped.

Initial scope is generation seed17, all348563 training records, all116314 development records and the same two fixed learners as H001 and H001C. No reserved data is read. Fit uses training only; proximity evaluation references development data but never feeds back into this fitted bandwidth. Retain neighbors, widths, pilot statistics, generation randomness, full tables, model files, predictions and complete table checks. Independently reconstruct generation, pilot fitting, table quality and student inference. Check all point distances using a different nearest neighbor implementation.

The fit stage is capped at12 CPU minutes. The whole job has20 minutes to allow a five minute audit and retention of complete or partial evidence. No paid compute or unlimited retries. Twelve implementation controls and six separate checker controls must pass before fitting. Retrieval and digest checks are required before any successor or result claim. This single seed extension is exploratory; it cannot establish multi seed reliability, scientific novelty, final performance, transfer or privacy.

Closest prior work: Segers, Sibuya and Tsukahara, The Empirical Beta Copula, arXiv1607.04430; Kojadinovic and Yi, smooth adaptive copula estimators, arXiv2106.10726; Chawla and colleagues, SMOTE, JAIR2002; statistical nearest neighbor bandwidth literature. No new dataset is introduced.
