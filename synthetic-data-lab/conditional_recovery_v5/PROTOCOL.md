# Conditional structure recovery experiment

This is an additive recovery from ba293ed81e218eac93f7e02abf32749ff9a6d328. The interrupted v4 run left a protocol, candidate selection record and manifest digest on GitHub, but its 807 referenced local artifacts are not present in the recovered workspace or available workflow artifacts. A digest is not a recoverable model. Historical chat scores and unretained v4 results will not serve as measured baselines.

The two original archives have been recovered and their exact SHA256 values checked. Covertype has 581012 source rows, with 348563 training, 116314 development and 116135 test rows. Feature identity hashes on compact int64 predictors give these partitions; labels do not enter the hash. Beijing has 420768 station hours, with 210240 before March 2015 for training, 105408 in the next year for development, and 105120 in the last year for testing. Missing values are represented by training medians and explicit masks. All eligible training records are used. Recovered counts and training medians agree with v3 metadata, but missing historical membership files prevent an exact historical split identity claim.

## Questions and bounded comparisons

Compare the retained random endpoint interpolation to exact nearest neighbor interpolation inside identical categorical bundles. Compare no calibration, pooled rank calibration, and contextual rank calibration using identical generated pools. Contexts are cover, wilderness and soil in Covertype, and station and month in Beijing. These are predefined contextual variables, not a claim that the population should be class balanced.

Include a conditional Gaussian copula control, a local partition Gaussian density with leaf targets 128 and 512, and a numerical diffusion residual model conditioned on fixed categorical contexts. Compare the diffusion residual to its Gaussian covariance initialization to test whether the neural component adds useful structure. These are local adaptations and combinations of known methods, not headline paper reproductions or claims of scientific priority. Optional geometric reconstruction is evaluated as a separate Covertype intervention, not bundled into a claim about calibration.

Generation seeds are 17, 29 and 41. Neural training uses eight complete epochs. At least one selected fit and repeated generation will be replayed. All variants use the full training partition and generate a matching number of rows. Student models use the same fixed LightGBM configuration. A fixed second learner and a Beijing PM10 excluded sensitivity check are included for serious finalists. Beijing is contemporaneous PM2.5 regression, not forecasting. No source row cap or subsampled final evaluation is permitted.

## Benchmark and selection

Measure hard validity, individual distributions, context specific distributions, training defined tails, query frequencies, copying, diversity, and downstream performance separately. Evaluate deterministic and aggregate metrics across complete generated tables. Expensive distance or structural checks may be restricted to declared finalist methods, not a hidden convenience sample of rows. Corrupted controls must fail the relevant dimension. A rule passing fraction is never an overall data quality percentage.

Development selection uses mean balanced accuracy for Covertype or mean absolute error for Beijing, with validity no worse than the real reference and copying below one percent. Do not call this utility selection a universal quality ranking. Report natural class frequencies and per class recall. Compare matched seeds and disclose compute. Volume checks at 0.25, 1, 2 and 5 times the training count are secondary and limited to a selected recipe when feasible.

Freeze source, configurations, split identities, generated tables, checkpoints and development selections before the final retained test evaluation. No subsequent generator tuning may use test outcomes. The exact activities of the lost local v4 process cannot be independently reconstructed; this run must not claim independent external confirmation of v4.

## Evidence and independence

Preserve all attempts and failures. New tests are separate from original tests. Artifact arithmetic and checkpoint inference are checked in a separate process without using the production scoring function. Same assistant authorship is not independent human or LLM agent review. No paid compute is authorized. New local evidence and publication receipts must distinguish actual remote commits from downloadable files. Completion requires retained final outcomes, not merely this protocol.
