# B002 contextual statistical reference

This bounded unit follows research_control/STATE.json at commit 1ce7d021ee2a448d3da979dc0e6c45c6150379cb. It is a development comparison, not a claim of novelty or final confirmation.

Use the complete H001 seed17 training and development arrays after archive and internal digest verification. Training has 348563 rows, development 116314 rows, and no reserved rows are scored. Fit a Gaussian copula separately in each exact wilderness, soil and cover context. Preserve every context count. Gaussian normal scores use average tied ranks, LedoitWolf covariance shrinkage after unit scaling, and an additional 0.000001 identity mixture. Draw correlated normal scores and invert empirical numerical marginals with interpolation and integer rounding. Report copied singleton contexts. This is a local standard statistical reference, not a published paper score reproduction.

Evaluate the same tree and linear student recipes used by H001 and H001C without new tuning. Retain all generated rows, fitted marginals, correlations, latent training scores, group identities, random draw order, model files, predictions, quality outputs, logs, and source. Recheck all H001 quality metrics with its separate verifier. Independently reconstruct generation and student inference. Existing tests are not changed. New control tests must pass before fit. Prior final exposure remains unknown, so development results cannot support final confirmation.

Initial scope is seed17 only, one CPU job, maximum 15 minutes. No paid compute or arbitrary data removal. Success permits a separately recorded seed extension and a focused mechanism hypothesis; it does not permit test set tuning. GitHub Actions retains the evidence for 90 days. Actual retrieval and file checks are required before the next unit. Failure artifacts remain visible.

Nearest prior art inspected: TabulaCopula official documentation on conditional Gaussian synthesis; Lee, Im and Kim, A Generalized Theory of Mixup for Structure Preserving Synthetic Data, arXiv:2503.02645; Zhang, Measuring the Dependency Gap, arXiv:2607.21636. Conditional statistics and variance correction are established ideas.
