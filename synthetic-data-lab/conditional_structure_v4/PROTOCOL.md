# Conditional structure repair study

Additive continuation from 157510d1bdbc51137d923fd1d43ecadacec62f3a. No original raw archive, historical experiment or main branch is to be changed. Local source archives match the SHA256 values recorded for Beijing and Covertype. The retained large study artifacts contain source code, schemas and temporal summaries, but not the complete historical per condition predictions and checkpoints. Historical chat scores are not treated as a verified baseline; all reported comparisons will be rerun.

## Questions

Test pooled versus context specific rank calibration on identical generated pools. Test locality against the old random endpoint interpolation with genuine nearest neighbor controls. Compare joint categorical diffusion with fixed categorical contexts and context covariance whitening. Include a conditional Gaussian with no neural denoiser to determine whether added neural machinery contributes. A covariance compensated interpolation is a further control, not an invention claim.

## Data

Use every eligible training record: 348563 Covertype rows and 210240 Beijing station hours. Full source releases contain 581012 and 420768 rows respectively. Covertype uses compact integer feature identity hashes modulo 100 with 60/20/20 bins. Its recovered partition counts and training medians match retained v3 metadata, but absent historical membership files prevent a byte identity claim. Beijing reuses training before March 2015, development before March 2016, and the final year as test. Keep missingness masks and disclose missing target exclusions.

## Gates

Development screening starts with seed 17. Repeat serious candidates with seeds 29 and 41. Preserve natural categorical frequencies rather than claiming class rebalancing reproduces the population. Neural screening uses eight complete epochs, with at most a fixed sixteen epoch extension selected using development evidence. Every neural epoch must visit every eligible row. No generator, transformation, neighbor search or development selection may read final test outcomes.

Use matched LightGBM students and a second tree ensemble sensitivity check. Classification primary utility is balanced accuracy; regression primary utility is MAE. Beijing is same hour PM2.5 regression using other contemporaneous measurements, not forecasting. Include a sensitivity check excluding PM10.

Rank variants within each family by average development utility only if hard validity is no worse than the real reference and exact copying is below one percent. Report marginal, conditional query, rare frequency, copying and near copying tradeoffs separately; this utility selection is not an overall quality certificate. Freeze candidates and decisions before final test scoring and never tune afterward. Use additional synthetic volume checks only where feasible and record omissions.

## Review

Invariant tests are written before candidate evaluation. Preserve existing source files. Deliberately corrupted controls must fail relevant benchmark dimensions. Independent metric implementation and checkpoint checks run in a separate process without calling the production scoring function. Same assistant authorship is not independent human review. Do not invent LLM agents or claim scientific novelty from a novel name or combination of known ideas.

Retain code, parameters, seeds, source and split hashes, models, generated tables, predictions, timing, failed runs and all development comparisons. Report measured gains and failures, including negative outcomes. No paid compute is authorized. This protocol records the ongoing development study before any final test scoring, not before all work in the project.
