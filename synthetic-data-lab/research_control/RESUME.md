# Resume instruction

Continue the synthetic data project using `synthetic-data-lab/research_control/STATE.json` and `PLAN.md` on branch `research/synthetic-data-reviewed-20260911` in `LinLee10/mess-of-projects`.

First resolve the current remote commit, read project operating instructions, and confirm that the recorded state still matches retained artifacts. Never overwrite uncommitted user work. Read only the chat excerpts, code and primary sources needed for the current unit. Do not restart the whole project.

Execute the next bounded work unit whose dependencies passed. If an actual recorded compute job is still running, inspect it rather than launch a duplicate. Preserve existing tests, isolate implementer and critic changes, and respect final test isolation. Do not use TinyFish or paid compute without authorization.

Write the hypothesis and expected outcome before an experiment. Save recoverable checkpoints and preserve failures. Independently verify the completed unit and confirm durable artifact retrieval, then atomically update state with exact artifact locations, commit and next action. Missing evidence remains missing; never reconstruct a score from chat claims.

Keep research direction open: reject or replace hypotheses when evidence warrants. Do not stop at the first negative result, but do not bundle an unlimited search into this invocation. End at the next safe checkpoint with a brief measured result, reliability status and next unit. A final response is not continuing background execution.

Stop and request the exact dataset before introducing any new dataset. Do not claim scientific novelty merely because the code or a result is new to this project.
